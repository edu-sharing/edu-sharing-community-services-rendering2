version: '3.8'

services:

  minio:
    image: bitnami/minio:latest
    ports:
      - "9110:9000"
      - "9111:9001"
    environment:
      - MINIO_ROOT_USER=adminuser
      - MINIO_ROOT_PASSWORD=adminpassword
      - MINIO_DEFAULT_BUCKETS=contentbucket,tempbucket,libbucket
    volumes:
      - "minio-data-volume:/bitnami/minio/data"

  repository-cache:
    image: docker.edu-sharing.com/community/common/edu_sharing-community-common-docker-redis:6.2.7
    environment:
      ALLOW_EMPTY_PASSWORD: "yes"
    ports:
      - "6379:6379"

  rabbitmq:
    image: rabbitmq:management
    ports:
      - "5672:5672"
      - "15672:15672"

  mongo:
    image: mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin
    ports:
      - "27017:27017"

  rendering2-document-converter:
    image: "community/edu_sharing-community-services-rendering2-deploy-docker-build-document-converter:maven-feature-renderer-2.0-SNAPSHOT"
    command:
      - -Xms${REPOSITORY_SEARCH_ELASTIC_TRACKER_JAVA_XMS:-512m}
      - -Xmx${REPOSITORY_SEARCH_ELASTIC_TRACKER_JAVA_XMX:-512m}
      - -Dcom.sun.management.jmxremote
      - -Dcom.sun.management.jmxremote.authenticate=false
      - -Dcom.sun.management.jmxremote.port=7199
      - -Dcom.sun.management.jmxremote.ssl=false
    ports:
      - "8081:8080"

volumes:
  minio-data-volume:
