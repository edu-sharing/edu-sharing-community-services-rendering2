<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.edu_sharing</groupId>
        <artifactId>edu_sharing-community-services-rendering2</artifactId>
        <relativePath>../pom.xml</relativePath>
        <version>git</version>
    </parent>

    <artifactId>edu_sharing-community-services-rendering2-admin-frontend</artifactId>
    <packaging>jar</packaging>

    <name>edu_sharing-community-services-rendering2-admin-frontend</name>

    <properties>
        <!-- Angular 20 erfordert Node >= 20.19 / 22.12 / 24.
             v22.17.0 ist im Artifactory-Raw-Mirror (raw/nodejs.org/dist) vorhanden. -->
        <node.version>22.17.0</node.version>
        <npm.version>10.9.2</npm.version>
        <!-- Der Angular-Workspace liegt unter src/main/frontend (Quellcode unter src/main). -->
        <frontend.dir>${project.basedir}/src/main/frontend</frontend.dir>
    </properties>

    <build>

        <plugins>

            <plugin>
                <groupId>com.github.eirslett</groupId>
                <artifactId>frontend-maven-plugin</artifactId>
                <configuration>
                    <nodeVersion>v${node.version}</nodeVersion>
                    <npmVersion>${npm.version}</npmVersion>
                    <workingDirectory>${frontend.dir}</workingDirectory>
                    <installDirectory>${frontend.dir}</installDirectory>
                </configuration>
                <executions>
                    <execution>
                        <id>admin-frontend-bootstrap</id>
                        <goals>
                            <goal>install-node-and-npm</goal>
                        </goals>
                        <phase>process-sources</phase>
                    </execution>
                    <execution>
                        <id>admin-frontend-install</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <phase>process-sources</phase>
                        <configuration>
                            <arguments>install</arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>admin-frontend-build</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <phase>compile</phase>
                        <configuration>
                            <arguments>run build</arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <configuration>
                    <filesets>
                        <fileset>
                            <directory>${frontend.dir}/dist</directory>
                        </fileset>
                        <fileset>
                            <directory>${frontend.dir}/node</directory>
                        </fileset>
                        <fileset>
                            <directory>${frontend.dir}/.angular</directory>
                        </fileset>
                    </filesets>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <executions>
                    <execution>
                        <id>package-sources</id>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <phase>package</phase>
                        <configuration>
                            <descriptors>
                                <descriptor>bin.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>

    </build>
</project>
